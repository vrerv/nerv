{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "yum install libuuid-devel libmount-devel zlib && yarn run build"
      }
    }
  ]
}
